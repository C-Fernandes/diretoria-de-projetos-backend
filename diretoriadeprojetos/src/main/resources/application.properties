spring.datasource.url=jdbc:postgresql://localhost:5432/diretoriadeprojetos
spring.datasource.username=postgres
spring.datasource.password=Clara.951
spring.datasource.driver-class-name=org.postgresql.Driver

# Garante a cria��o das tabelas
spring.jpa.hibernate.ddl-auto=update

# Dialeto do PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.config.import=optional:classpath:secret.properties


spring.mvc.throw-exception-if-no-handler-found=true

# Desativa o mapeamento padrão de recursos do Spring para garantir que ele use nossa exceção
spring.web.resources.add-mappings=false
